/**
 * Finds the element that is num spaces away from the last node of the linked list
 *
 * @param list
 * @param num
 * @returns {Node} Element that num spaces away from last node of list
 */
export function fromLast(list, num) {
	return 0;
}
