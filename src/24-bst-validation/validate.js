/**
 * Validates the binary search tree by ensuring that
 * every node's left hand child is less than the
 * parent node's value, and every node's right hand child
 * is greater than the parent
 *
 * @param node
 * @param min
 * @param max
 * @returns {boolean}
 */
export function validate(node, min = null, max = null) {
	return false;
}
