import Queue from './queue';

/**
 * Merges the contents of two queues into a new queue
 *
 * @param queue1
 * @param queue2
 * @returns {Queue} A new queue with contents of both queue1 and queue2
 */
export function weave(queue1, queue2) {
	let queue = new Queue();
	return queue;
}
