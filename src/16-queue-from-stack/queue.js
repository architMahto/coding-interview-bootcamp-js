/** Class representing a queue data structure */
class Queue {
	/**
	 * Creates a queue
	 */
	constructor() {}

	/**
	 * Adds an element to the queue
	 *
	 * @param element
	 */
	add(element) {}

	/**
	 * Finds the first element from the queue without removing it
	 *
	 * @returns {any} The first element from the queue
	 */
	peek() {}

	/**
	 * Removes the first element from the queue
	 *
	 * @returns {any} The first element
	 */
	remove() {}
}

export default Queue;
