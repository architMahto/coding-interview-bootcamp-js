/**
 * Creates an array where each element of a tree is the width
 * of the tree at each level
 *
 * @param root
 * @returns {Array} Array that is the same width of the tree at each level
 */
export function levelWidth(root) {
	return [];
}
