/** Class representing a stack data structure */
class Stack {
	/**
	 * Creates a stack
	 */
	constructor() {}

	/**
	 * Finds the last element without removing it
	 *
	 * @returns {any} The last element of the stack
	 */
	peek() {}

	/**
	 * Removes the last element from the stack
	 *
	 * @returns {any} The last element of the stack
	 */
	pop() {}

	/**
	 * Adds element to the stack
	 *
	 * @param element
	 */
	push(element) {}
}

export default Stack;
