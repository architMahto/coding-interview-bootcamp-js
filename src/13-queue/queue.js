/** Class representing a queue data structure */
class Queue {
	/**
	 * Creates a queue
	 */
	constructor() {}

	/**
	 * Adds a record to the queue
	 *
	 * @param element
	 */
	add(element) {}

	/**
	 * Removes the first element the queue
	 *
	 * @returns {any} The first element from the queue
	 */
	remove() {}
}

export default Queue;
